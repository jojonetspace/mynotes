
todo:
qty decimal(14,4)
formatNumericInput 作为纯工具函数抽到 listing-common.js 公共模块

loadLogistic 看看是否能将多仓的接口分离出来，这样就可以把保留 `logisticConfig` 为 Standard data 属性移入物流组件内部去了

待移除
rules.positiveWeight
rules.positiveInteger
### 模块划分总览

| 模块              | 当前位置 (约)                     | 独立性 | 建议方式     | 预计行数   |
| --------------- | ---------------------------- | --- | -------- | ------ |
| 1. 促销价 (Shopee) | 模板+JS 分散多处                   | 高   | 拆为组件     | ~300行  |
| 2. 物流属性         | 模板 ~700-830行, JS 多处          | 中高  | 拆为组件     | ~500行  |
| 3. 规格属性 (变体面板)  | 模板 ~270-650行, JS ~3200-4050行 | 中   | 拆为组件     | ~1200行 |
| 4. 商品属性渲染       | 模板 ~80-115行, JS 少量           | 中高  | 拆为组件     | ~200行  |
| 5. 产品认证         | 模板 ~115-140行, JS ~2350-2450行 | 高   | 拆为组件     | ~250行  |
| 6. 校验方法集合       | JS ~1750-2000行               | 高   | 提取到独立 JS | ~300行  |
| 7. 属性合并逻辑       | JS ~2450-2700行               | 高   | 提取到独立 JS | ~350行  |

阶段 1（快速见效）： ├── 提取 standard-validators.js（纯重构，无风险） ├── 提取 ProductCertifications.cshtml（最独立） └── 提取 PromotionPrice.cshtml（Shopee 专属，隔离性好） 阶段 2（中等工作量）： ├── 提取 LogisticsPanel.cshtml └── 提取 standard-attributes.js 阶段 3（较大重构）： └── 提取 SpecificationPanel.cshtml（含 SKU 表格）

**测试：**
1428
前置条件：
1）关闭店铺管理中的上传库存功能
2）G.RefreshShopToken 禁用这个job
线上3pf的店铺：select * from gcommon.dbo.shop where co_id=100000352 and shop_id=899263602

skinXX.oo
易途
亿里

100000352	深圳市易途国际贸易
100000499	义乌宜品供应链
100000570	Jago Pipa
100000577	聚水潭軟體(台灣)有限公司
100000609	四個灣仔碼頭有限公司
15984969

命名定义：
saleproperty（类目属性）:颜色尺寸
productproperty（商品属性）:材质
otherproperty:其他属性

1.商品上传
2.商品下载，
	判断data是否有值，有值的话说明是本地的商品（默认有category_rules），从平台下载的时候覆盖data并保留category_rules
3.商品编辑
	店铺授权失效：
		1.如果有data，并且有category_rules的话，可以打开页面并查看，否则直接提示商品信息不全，不要打开页面了
	店铺授权有效的话：
		从平台下载category_rules
4.商品复制
	1）相同的判断data和category_rules，如果有值那么可以复制，否则提示商品信息不全
	2）目标店铺必须是授权的店铺（过滤失效的店铺），然后将对应的数据复制过去，需要注意3）必须清理平台上的一些id值，category、类目属性、属性值都需要自动映射（从商家映射数据、系统映射数据、系统临时计算（向量计算或者ai接口处理））三层来处理
5.映射类目、类目属性、类目属性值、商品属性、商品属性值
	1）


1）我现在接到一个任务，需要优化商品刊登中用于复制商品时类目、类目属性、商品属性及其值的映射基础数据的提供方案，请帮我看下现有的映射表的数据是怎么来的，目前做到了哪些？现在的要求是复制商品的时候同平台的除了lazada外，shopee和tiktok的类目和属性都是相同的，应该可以直接复制，不同平台复制的时候就需要用到商品映射了。映射的逻辑需要分为几步动作，先从映射表中获取商家级别的数据，其次从映射表中获取系统级别的数据（目前系统级别是存在商家id为2下面），如果系统级别的数据也没有的话，那么需要系统自动计算推荐（目前好像有个向量的计算）。首先确认下目前这些系统中是否已经都有处理了。
2）商家级别的数据和系统级别的数据现在是怎么来的

Skills:
	自动触发（基于 keywords 匹配）：

- 你说 "对接 Mirakl 平台刊登" / "新增平台商品发布" / "刊登接入新平台" → 自动激活主 Skill
- 你说 "刊登批量修改适配" / "商品批量操作" → 自动激活批量操作 Skill
- 你说 "刊登列表页适配" / "刊登 Index 页面" → 自动激活列表页 Skill

活动相关job：
	G.DiscountActivityShopSync：折扣活动列表扫描入队-s
	G.DiscountActivityQueue: 折扣活动队列消费-s
	G.DiscountActivityAutoExtend: 折扣活动自动续期-c

刊登相关job：
	G.API.ShopItemDownTask
	G.API.ShopItemQueueTask
	G.API.ShopItemConvertTask
	G.ItemDown
	G.ItemQueue
	G.ItemConvert
	G.API.ResourceUpload
	G.API.GlobalPublishResult //全球店铺子店铺轮询
	
Shopee商品置顶:
	G.ShopeeBoost

ItemConvertBase
层级	IsListing 控制的内容	位置
接口层	是否调用额外的刊登接口（如全球商品获取子店铺发布状态）	ItemDownService 的 EnsureItem/EnsureGlobalItem
转存层	IsListing 或 ListingEdited=true 时，才将刊登数据写入 data	ItemConvertBase 的 if (dbShopItem.ListingEdited || IsListing) → MergeData


项目中其他模块的并行处理模式总结
通过查看订单、物流、库存等模块的代码，项目中 Parallel.ForEach 的使用有一个统一的模式：

模式：并行内只做平台 API 调用，不做数据库操作
模块	并行内做什么	数据库操作在哪里
Shopee OrderDownService	调用 shopeePlatform.GetTrackingNumber() 获取物流单号	并行外统一处理
Shopee OrderQueueService	调用 shopeePlatform.GetTrackingNumber() 获取物流单号	并行外统一处理
Shopee/Lazada OrderProcessService	调用平台 API 发货（NormalOrderShip、Pack、ReadyToShip）	并行外 HandleWaitTodo 统一回写
ByQueueTaskBase	PageProcess() 处理数据（调平台 API）	并行结束后调 Save() 统一写库
DownloadByQueueBase	DownloadSingle() 调平台 API 下载	并行结束后调 SaveData() 统一写库
DrpStockCalcService	纯内存计算（CalcSingleDrpStock）	不涉及数据库

**日志：**
工作原理
后端添加日志：通过 CurrentUserContext.AddClientLog(log, isShow) 或 MasterService.AddClientLog(log, isShow) 添加
自动序列化：PageModelBase 的 Success()/Error() 等方法会自动调用 .AppendClientLog(GetClientLog())，将 ClientLog 列表附加到 JSON 响应的 client_log_ 字段
前端自动输出：$call 函数在收到响应后检查 rv.client_log_，如果有内容就 console.log(rv.client_log_) 输出到浏览器控制台
使用方式
在 Service 层中直接调用：

// 在 ShopItemService 中（继承自 MasterDABase，有 CurrentUserContext）
CurrentUserContext.AddClientLog("日志内容", true);

// 或通过 MasterService
MasterService.AddClientLog("日志内容", true);

项目中没有专门控制 ClientLog 输出的全局开关，但有以下机制可以利用：

ClientLogItem.show 字段：控制是否在 UI 上弹出提示（show=false 只输出到 console，不弹提示）
IsTempOpen 临时开关：可以通过后台配置页面（Admin/Config/TempOpen）按商家 ID 控制是否开启某个功能，适合做"是否输出性能日志到 ClientLog"的开关
建议方案：用 IsTempOpen("perf_client_log") 作为开关，只有在后台配置了当前商家 ID 时才输出 StepTimer 结果到 ClientLog。这样：

线上默认不输出，不影响正常用户
你调试时在 TempOpen 后台加上自己的 co_id 就能看到
排查完毕后移除即可

并发：
![[Pasted image 20260508093729.png]]
![[Pasted image 20260508093747.png]]
